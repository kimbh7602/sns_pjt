<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssafy.food">

	<insert id="insert" parameterType="FoodVo">
		insert into food 
		values(
			#{code},
			#{name},
			#{supportpereat},
			#{calory},
			#{carbo},
			#{protein},
			#{fat},
			#{sugar},
			#{natrium},
			#{chole},
			#{fattyacid},
			#{transfat},
			#{maker},
			#{material},
			#{img},
			#{allergy},
			#{searchCount}
		)
	</insert>

	<update id="update" parameterType="FoodVo">
		update food set 
			name=#{name},
			supportpereat=#{supportpereat},
			calory=#{calory},
			carbo=#{carbo},
			protein=#{protein},
			fat=#{fat},
			sugar=#{sugar},
			natrium=#{natrium},
			chole=#{chole},
			fattyacid=#{fattyacid},
			transfat=#{transfat},
			maker=#{maker},
			material=#{material},
			img=#{img},
			allergy=#{allergy},
		where
			code=#{code}
	</update>

	<delete id="delete" parameterType="FoodVo">
		delete from food 
		where
		code=#{code}
	</delete>

	<select id="selectList" resultType="FoodVo">
		select *
		from food
	</select>

	<select id="selectOne" parameterType="int"
		resultType="FoodVo">
		select *
		from food
		where 
			code = #{code}
	</select>
	
	<select id="proSearchByName" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where name like concat('%', #{keyword}, '%')
	</select>
	
	<select id="proSearchByMake" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where maker like concat('%', #{keyword}, '%')
	</select>
	
	<select id="proSearchByMaterial" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where material like concat('%', #{keyword}, '%')
	</select>
	
	<select id="allergyinfo" parameterType="string" resultType="string">
		select allergy
		from user
		where id=#{id}
	</select>
	
	<update id="searchCountUp" parameterType="int">
		update food
		set searchCount = searchCount+1
		where code = #{code}
	</update>
	
	<select id="searchByNameToFirst" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where name like concat(#{keyword}, '%')
	</select>
	
	<select id="searchByMakeToFirst" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where maker like concat(#{keyword}, '%')
	</select>
	
	<select id="searchByMaterialToFirst" parameterType="string" resultType="FoodVo"> 
		select *
		from food
		where material like concat(#{keyword}, '%')
	</select>

</mapper>